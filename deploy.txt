files :	bot.py
	Procfile	 : web: python3 bot.py
	requirements.txt : python-telegram-bot==12.7

# deploy on heroku

# one time (login for heroku cli)
heroku login

# create a new webapp [ set the URL in updater.bot.setWebhook() ]
heroku create

git init
git add .
git commit -m "first commit"

# psuh the files
heroku git:remote -a YourAppName

# deploy app
git push heroku master

# app stop after 24 hrs inactivity (add crap to make it work again)
git add .
git commit -m "changing python3 to python in Procfile"
git push heroku master

# delete app
heroku apps:destroy	# or from web interface

